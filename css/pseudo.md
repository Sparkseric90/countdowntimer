Set const display 
set varGoal = if user input, then input;
    else display "desired date"
    set days = 'd';
    set hours = 'h';
    set minutes = 'm';
    set seconds = 'S';
     else display = "Times up!"

